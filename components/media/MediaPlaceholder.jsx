/**
 * Legacy placeholder kept for structure tests and docs links.
 * Phase 3 media UI lives in MediaRoom / RoomWorkspace.
 */
export function MediaPlaceholder() {
  return (
    <div className="panel">
      <p>
        Media connects after you join with a valid invitation.
        The LiveKit workspace mounts in this room once a participant token is issued.
      </p>
    </div>
  );
}
